# App (host local — fora do Docker)
PORT=3000
HOST=0.0.0.0
COLLECTOR_MODE=mock

# Infra (host local — portas expostas pelo docker-compose)
KAFKA_BROKERS=localhost:9094
POSTGRES_URL=postgresql://cmip:cmip@localhost:5432/cmip
REDIS_URL=redis://localhost:6379
